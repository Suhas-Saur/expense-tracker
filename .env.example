# Student Income & Expense Tracker - Environment Configuration
# This is a client-side SPA with browser localStorage persistence.
# No sensitive backend secrets or external database credentials are required.

# Optional application configuration:
VITE_APP_TITLE="Student Finance Tracker"
VITE_APP_CURRENCY_SYMBOL="₹"
VITE_APP_ENV="production"
